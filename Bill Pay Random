# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")
# 🚨 Don't change the code above 👆

#Write your code below this line 👇

import random

#this will select a random name in the names list and display that to the user.
print(f"It\'s {random.choice(names)}\'s turn to buy today.")
